to run unix socket server, goto server folder and then execute ./server.sh

to run unix socket client, goto client folder and then execute ./client.sh 

currently the server will just dump out the message written from client

  
